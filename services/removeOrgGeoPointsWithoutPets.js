const fs = require("fs");const path = require("path");const readline = require("readline");const dataLocation = "../public/PetsData";const setting = require("../config/appSetting");const keys = require("../config/credential");const firebase = require('firebase-admin')const removeOrgGeoPointsWithoutPets = (realtimeDB,firestore) => {  const docAmountPerBatch = setting.firestore.docAmountPerBatch;  const geoRef = realtimeDB.  const docRef = firestore.collection(setting.firestore.petsCollectionName);}module.exports = removeOrgGeoPointsWithoutPets