const fs = require("fs");const readline = require("readline");const dataLocation = "./public/PetsData";const allPetDataUploadToDatabase = () => {  let fileIndex = 1;  let fileName = "KJquDhsf_pets_" + fileIndex;  while (fileIndex > 0) {    const rl = readline.createInterface({      input: fs.createReadStream(dataLocation + fileName)    });    let i = 0;    rl.on("line", line => {      let jsonBuffer = JSON.parse(line);      console.log(jsonBuffer.animalID);      // docRef      //   .doc(jsonBuffer.animalID)      //   .set(JSON.parse(line))      //   .then(ref => {      //     console.log("added doc with", ref);      //   });      i++;    });  }};module.exports = allPetDataUploadToDatabase;